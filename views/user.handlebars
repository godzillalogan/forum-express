
<div class="card-body shadow row d-flex justify-content-around align-items-center">
  <div class="col-lg-4" >
    <img class="rounded-circle"src="{{user.image}}" alt="" style="width: 300px; height:300px">
  </div>
  <div calss="col-lg-8">
    <h1>{{user.name}}</h1>
    <p>{{user.email}}</p>
    <a href='/users/{{user.id}}/edit'>
      <button type="button" class="btn btn-info">編輯</button>
    </a>
    <div class="mt-3">
      <a href="/">回上一頁</a>
    </div>
  </div>
</div>
